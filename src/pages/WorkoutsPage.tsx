import { Box, Typography } from '@mui/material';
export default function WorkoutsPage() {
  return <Box><Typography variant="h4" fontWeight={700}>Workouts</Typography><Typography color="text.secondary" sx={{ mt: 1 }}>Log and track your workout sessions here.</Typography></Box>;
}
