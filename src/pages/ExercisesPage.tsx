import { Box, Typography } from '@mui/material';
export default function ExercisesPage() {
  return <Box><Typography variant="h4" fontWeight={700}>Exercise Library</Typography><Typography color="text.secondary" sx={{ mt: 1 }}>Browse and search exercises here.</Typography></Box>;
}
