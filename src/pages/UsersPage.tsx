import { Box, Typography } from '@mui/material';
export default function UsersPage() {
  return <Box><Typography variant="h4" fontWeight={700}>User Management</Typography><Typography color="text.secondary" sx={{ mt: 1 }}>Admin: manage all platform users here.</Typography></Box>;
}
